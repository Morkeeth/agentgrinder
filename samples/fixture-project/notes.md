# fixture project

This file exists so `agentgrinder coach samples/sample_session.jsonl` has one artifact that is
really on disk. The transcript beside it also names `draft.md`, which does not exist, so the
coach reports one artifact produced of two written. Neutral content, no person, no repository
of anyone's.
